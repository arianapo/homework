function obtenerResto(x, y) {
 
  return x % y;
    console.log(obtenerResto(x, y)); // 0
    x=5;
    y=8;
    console.log(obtenerResto(x, y)); // 1.6
    console.log(obtenerResto(5, 5)); // 0

}
